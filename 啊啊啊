【问题描述】
小 Y 最近发现街上的汽车越来越多了，作为汽车的重要标志——汽车牌照也是越来越不够用了，已经从以前的十进制发展到三十六进制了，以前的一个汽车牌照“苏 D88888”，现在的牌照“苏 D0YY11”。
小 Y 突发其想，想知道他看到的大量汽车牌照中最近的两个汽车牌照相差多少？
【输入格式】
若干行(不超过 500000 行)，每行为一个汽车牌照。
每个汽车牌照为一个 7 位的字符串，格式为 SD×××××，其中一个 × 表示一个 0~9 或A~Z，所涉及的字母均为大写。
【输出格式】
一行一个数，表示最接近的两个汽车牌照之间的差值，要求为十进制数。
【输入样例】
SD12345
SD88888
SD22222
SD99999
【输出样例】
1678245

#include<bits/stdc++.h>
using namespace std;
int main(){
	//freopen("lisence.in" ,"r" ,stdin);
    //freopen( "license.out" ,"w" ,stdout);
    long long n=36,ans = 0,i = 2,p=0;
    string s;
    long long a[1000]={0};
    long long m=0;
    while(getline(cin,s)){
		for(int l=2;l<7;l++){
			
		
          if(s[l]>='0'&&s[l]<='9'){
          ans = ans * n + (s[l] - 48);
     	 }
     	 else {
     	 	ans = ans * n + (s[l] - 'A')+10;  
     	 }
    	} 
    	a[p]=ans;
    	ans=0;
    	p++;
	}
    sort(a,a+p);
    long long min=1e9;
    for(int i=1;i<p;i++){
    	if((a[i]-a[i-1])<min) min=a[i]-a[i-1];
	}
    cout<<min;
    return 0;
}
